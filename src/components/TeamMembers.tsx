import React from "react";
import "./teamMembers.scss";

type TeamMembersProps = {
  children?: React.ReactNode;
};

export const TeamMembers: React.FC<TeamMembersProps> = (): JSX.Element => {
  return <div></div>;
};
